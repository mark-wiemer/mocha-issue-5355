{
    "name": "mocha-watch-mcve",
    "dependencies": {
        "mocha": "11.2.2"
    },
    "scripts": {
        "test:base": "mocha --watch file.spec.js",
        "test:watch:absolute": "npm run test:base -- --watch-files=$(realpath internal-dep.js)",
        "test:watch:parent": "npm run test:base -- --watch-files=$(realpath ../dependency/dep.js)"
    }
}
